name: Hello world

on:
  push:
    branches: ["main"]

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Hello world
        shell: bash
        run: |
          echo "Hello Ahmad and Maryam"
